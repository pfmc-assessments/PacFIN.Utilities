#' @param xlab,ylab A character value specifying the label for an axes of
#' a figure. This is typically passed to [ggplot2::xlab] or [ggplot2::ylab]
#' but it can also be passed to the `xlab` or `ylab` arguments of [plot].
#' No additional formatting will take place.
